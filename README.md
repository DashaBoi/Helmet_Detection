# Helmet_Detection
Big Project Artificual Intelligence

Khi bắt đầu sử dụng, hãy chạy dòng sau các dòng sau

1. pip install roboflow supervision gradio opencv-python pillow numpy
2. pip install ultralytics

Sau đó nếu muốn sử dụng model YOLOv26s thì xóa comment, tương tự cho RF-DETR

3. Chạy python demo.py để chạy chương trình
4. Ấn vào local URL để chạy chương trinh
5. Chọn một ảnh giao thông để thử